apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: rag-platform
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s
    scrape_configs:
      - job_name: 'rag-api'
        static_configs:
          - targets: ['api-service:8000']